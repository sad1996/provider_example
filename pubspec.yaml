name: flutter_arch
description: An example flutter architecture application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  connectivity: ^0.4.4
  provider: ^3.1.0
  shimmer: ^1.0.0
  http: ^0.12.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true